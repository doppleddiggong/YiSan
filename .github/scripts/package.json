{
  "name": "agentrule-devlog",
  "version": "1.0.0",
  "description": "DevLog generation scripts for AgentRule",
  "main": "generate-devlog.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "openai": "^4.20.1"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
